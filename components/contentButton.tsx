import React from "react";

type Props = {};

const contentButton = (props: Props) => {
  return <div>


    <Button   type="buton" onClick={ ()=> router.push("/syllabus") } ></Button>
  </div>;
};

export default contentButton;
